const User = require('../models/User');


exports.addProfile = (req, res) => {
    const { name, email, hero, bio } = req.body;
    let profilePic;
    let resume;


    try {
    

    } catch (error) {
        
    }
}